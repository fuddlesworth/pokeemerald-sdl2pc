name: CI

on:
  push:
    branches: [ pc_port ]
  # Any base branch, so pull requests stacked on other pull requests get checked too
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GAME_VERSION: EMERALD
      GAME_REVISION: 0
      GAME_LANGUAGE: ENGLISH
      MODERN: 0
      COMPARE: 1
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Checkout agbcc
        uses: actions/checkout@master
        with:
          path: agbcc
          repository: pret/agbcc

      - name: Install binutils
        run: |
          sudo apt update
          sudo apt install \
            gcc-arm-none-eabi \
            binutils-arm-none-eabi \
            g++-mingw-w64-i686 \
            g++-mingw-w64-x86-64 \
            libpng-dev \
            libsdl2-dev \
            gdb
        # build-essential and git are already installed
        # gcc-arm-none-eabi is only needed for the modern build
        # as an alternative to dkP

      - name: Install SDL2 MinGW libraries
        run: |
          curl -fsSL https://archive.org/download/sdl-2-2.0.16/SDL2-devel-2.0.16-mingw.tar.gz \
            | tar -xz
          mv SDL2-2.0.16 SDL2

      - name: Install agbcc
        run: |
          ./build.sh
          ./install.sh ../
        working-directory: agbcc

      - name: agbcc
        env:
          COMPARE: 0
        run: make -j${nproc} all syms

      - name: Modern
        env:
          MODERN: 1
          COMPARE: 0
        run: make -j${nproc} all

      - name: Windows (64-bit)
        env:
          IS64BIT: 1
        run: make -j${nproc} winwsl

      - name: Windows (32-bit)
        env:
          IS64BIT: 0
        run: make -j${nproc} winwsl

      - name: Linux (64-bit)
        env:
          IS64BIT: 1
        # Debug info (it doesn't change the code) is needed by the data audit below
        run: make -j${nproc} linux CFLAGS=-g

      - name: Headless tests (Linux 64-bit)
        run: python3 test/headless/smoke.py ./pokeemerald64 --audit --out build/headless

      - name: Upload headless test output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: headless-linux64
          path: build/headless

      #- name: Linux (32-bit)
      #  env:
      #    IS64BIT: 0
      #  run: make -j${nproc} linux
